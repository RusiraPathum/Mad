<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.boadme">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".BoardingList_homepage">

        </activity>
        <activity android:name=".BoardingHome">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".updateB" />

        <activity android:name=".getDetails" />
        <activity android:name=".Hostal_listviewActivity" />
        <activity android:name=".Edit_hostalActivity" />
        <activity android:name=".Addhostal" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>